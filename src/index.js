//Import the files that are needed
import react from "react";
import reactDOM from "react-dom";
import App from "./App";

//Render the file to the root element
ReactDOM.render(<App />, document.getElementById("root"));
